import 'package:flutter/material.dart';
class Exams_Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Exams Page'),
      ),
      body: Container(
          child: Center(
            child:  Text('Exams Page'),
          )
      ),
    );
  }
}
